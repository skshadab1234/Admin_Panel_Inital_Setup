import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
    title: {
        template: 'Dashboard | Siyafy',
        default: 'Dashboard',
    },
};

const LandingPage = ({ params }: any) => {
    return (
        <div>
        </div>
    );
};

export default LandingPage;
